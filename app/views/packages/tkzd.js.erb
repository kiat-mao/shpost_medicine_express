var ready;

ready = function() {
	$.ajax({
		type : 'GET',
		url : '/packages/zxqd/',
		data: { package_id: $('#package_id').val()},
		dataType : 'script'
	});
}

$(document).ready(ready);
$(document).on('page:load', ready);